import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
    //toolbar: theme.mixins.toolbar,
    content: {
      padding: '50px',
      margin: '0 auto',
    },
    toolbar: {
        height: '80px',
    },
  }));